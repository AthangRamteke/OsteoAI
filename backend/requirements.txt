# OsteoAI backend dependencies.
# Keep the existing ML/runtime versions aligned with the frozen model artifact.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.20,<1.0

scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3
shap==0.51.0

# Document extraction
pypdf>=6.0,<7.0
python-docx>=1.1,<2.0

# Image OCR support (requires the Tesseract OCR engine to be installed
# separately on Windows and available on PATH).
Pillow>=10,<12
pytesseract>=0.3.13,<0.4
